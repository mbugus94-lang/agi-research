# Build Log - 2026-07-31 - Multi-Schema-Invalid Markdown Review Assertion

## Status

Complete. Added a deterministic CLI regression for the existing `multi-schema-invalid` review-only fixture. Focused review/advisory regression passes 17/17.

## Research synthesis

Recent agent research emphasizes lossless context management, falsifiable long-horizon commitments, explicit memory associations, externalized program state, and safety controls for self-evolving systems. The build applies those principles to the CAGE-1 report boundary: every invalid member remains tied to its source and physical line, and the Markdown projection is tested as an operator-visible contract.

## Implementation

- Added one test to `experiments/test_cage1_review_decision.py`.
- The test invokes `cli.cage1_review --fixture multi-schema-invalid --format markdown`.
- It asserts deterministic finding order for `member-z.jsonl`, `member-a.jsonl`, and `member-m.jsonl`, physical lines 12/4/19, both parser reasons, critical/escalate output, operator review requirement, and no automatic remediation.

## Safety boundary

Test-only and review-only. No policy, decision, evidence repair, action application, or self-modification is performed.

## Validation

- `python -m pytest -q experiments/test_cage1_review_decision.py experiments/test_cage1_advisory.py` -> 17 passed.
- Changed modules compile.
- `git diff --check` passes.
- Manual Markdown CLI output verified.

## Next priority

Add a review-only Markdown assertion for the mixed-fleet fixture, preserving the distinction between valid decision evidence and schema-invalid evidence.
